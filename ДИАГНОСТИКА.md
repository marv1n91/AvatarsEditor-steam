# 🔍 Диагностика проблем с авторизацией

## Шаг 1: Проверьте наличие файлов

### Проверьте .maFile:

Файл `vcaox5993.maFile` должен находиться в папке `mafiles/`:

```
avatarsscript/
├── mafiles/
│   └── vcaox5993.maFile  ← Здесь должен быть ваш файл!
```

**ВАЖНО:** Убедитесь что файл называется ТОЧНО `vcaox5993.maFile`, а не `vcaox5993.maFile.txt`

### Проверьте accounts.txt:

Файл `accounts/accounts.txt` должен содержать:
```
vcaox5993:wkxho8755:vcaox5993.maFile
```

### Проверьте аватарки:

В папке `avatars/` должны быть изображения (JPG, PNG, GIF):
```
avatars/
├── avatar1.jpg
├── avatar2.png
└── ...
```

---

## Шаг 2: Запустите диагностический скрипт

Этот скрипт откроет браузер **С ВИДИМЫМ ОКНОМ**, чтобы вы могли видеть что происходит:

```bash
python test_login.py
```

### Что вы увидите:

1. **Окно Chrome откроется** - вы увидите процесс авторизации
2. **Автоматический ввод** - логин и пароль введутся сами
3. **Steam Guard код** - если требуется, код сгенерируется и введется
4. **Результат** - браузер останется открытым 10-30 секунд для проверки

### Возможные проблемы:

#### ❌ "Privacy error" или "Access denied"
- **Решение:** Запустите на локальной машине, а не в Docker
- Steam может блокировать доступ из подозрительных сетей

#### ❌ ".maFile не найден"
- **Решение:** Положите файл `vcaox5993.maFile` в папку `mafiles/`
- Проверьте название файла (без лишних расширений!)

#### ❌ "Неверные учетные данные"
- **Решение:** Проверьте правильность логина и пароля
- Убедитесь что аккаунт не заблокирован

#### ❌ "Требуется код Steam Guard, но shared_secret не предоставлен"
- **Решение:** Проверьте что .maFile содержит поле `shared_secret`
- Откройте .maFile в текстовом редакторе и убедитесь что там есть JSON с полем `shared_secret`

---

## Шаг 3: Проверьте .maFile

Откройте файл `mafiles/vcaox5993.maFile` в текстовом редакторе. Он должен выглядеть примерно так:

```json
{
  "shared_secret": "ваш_секретный_код_в_base64==",
  "identity_secret": "другой_код_в_base64==",
  "account_name": "vcaox5993",
  "Session": {
    "SteamID": "76561199..."
  }
}
```

**ВАЖНО:** Поле `shared_secret` должно быть заполнено!

---

## Шаг 4: Запустите основную программу

После успешной диагностики:

```bash
python main.py
```

Для запуска с видимым браузером (для отладки), измените в `main.py` строку 156:

```python
steam_manager = SteamManager(headless=False)  # Браузер будет виден
```

---

## 🆘 Дополнительная помощь

### Включите подробное логирование:

В `config.ini` установите:
```ini
log_level = DEBUG
```

### Проверьте логи:

После запуска проверьте файл `logs/app.log`

---

## ✅ Контрольный список:

- [ ] Chrome/Chromium установлен
- [ ] Файл `vcaox5993.maFile` в папке `mafiles/`
- [ ] Файл содержит `shared_secret`
- [ ] Файл `accounts/accounts.txt` заполнен правильно
- [ ] В папке `avatars/` есть изображения
- [ ] Интернет-соединение работает
- [ ] Steam не блокирует ваш IP

---

**Если проблема сохраняется**, запустите `test_login.py` и отправьте скриншот окна браузера и вывода консоли.
