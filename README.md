# Steam Avatar Auto-Changer

Автоматическая программа для смены аватарок на множестве Steam аккаунтов. Поддерживает загрузку 30-40+ аккаунтов и случайное назначение аватарок из указанной папки.

## Возможности

- Автоматическая авторизация на нескольких Steam аккаунтах
- Случайное назначение аватарок из папки
- Поддержка Steam Guard (shared secret)
- Обработка 30-40+ аккаунтов за один запуск
- **Настраиваемая задержка между аккаунтами** (по умолчанию 60 секунд / 1 минута)
- **Визуальный таймер обратного отсчета** с прогресс-баром
- **Конфигурационный файл** для гибкой настройки
- **Аргументы командной строки** для быстрых изменений
- Подробное логирование всех операций
- Автоматическая валидация изображений
- Цветной вывод в консоль для удобства
- Расчет времени выполнения и прогноз завершения

## Требования

- Python 3.7+
- Google Chrome или Chromium браузер
- Steam аккаунты
- Изображения для аватарок (JPG, PNG, GIF, BMP)

## Установка

### 1. Клонирование репозитория

```bash
git clone https://github.com/yourusername/avatarsscript.git
```

### 2. Установка Google Chrome

Программа использует Google Chrome для автоматизации входа в Steam.

**Windows:**
- Скачайте и установите Google Chrome: https://www.google.com/chrome/

### 3. Установка зависимостей Python

```bash
pip install -r requirements.txt
```

**Важно:** ChromeDriver будет установлен автоматически при первом запуске через Selenium. Если возникнут проблемы, убедитесь что версия Chrome актуальная.

## Настройка

### 1. Добавление аккаунтов

Создайте файл `accounts/accounts.txt` и добавьте ваши аккаунты в одном из форматов:

**Формат с Steam Guard (shared secret):**
```
username1:password1:SHAREDKEY123
username2:password2:SHAREDKEY456
username3:password3:SHAREDKEY789
```

**Формат с .maFile (Steam Desktop Authenticator):**
```
username1:password1:username1.maFile
username2:password2:mafiles/username2.maFile
username3:password3:username3.maFile
```

### 2. Добавление аватарок

Поместите изображения, которые вы хотите использовать в качестве аватарок, в папку `avatars/`.

**Поддерживаемые форматы:**
- JPG / JPEG
- PNG
- GIF
- BMP

**Требования к изображениям:**
- Рекомендуемый размер: 184x184 пикселей
- Максимальный размер: Steam автоматически изменит размер больших изображений

### 3. Настройка задержки (опционально)

Файл `config.ini` позволяет настроить задержку между обработкой аккаунтов:

```ini
[Settings]
# Задержка между обработкой аккаунтов (в секундах)
# Рекомендуется: 60 секунд (1 минута) или больше
delay_between_accounts = 60

# Задержка после авторизации перед сменой аватарки (в секундах)
delay_after_login = 2

# Показывать таймер обратного отсчета
show_countdown = true

# Уровень логирования (DEBUG, INFO, WARNING, ERROR)
log_level = INFO
```

## Использование

### Базовое использование

Просто запустите главный скрипт:

```bash
python main.py
```

Программа автоматически:
1. Загрузит все аккаунты из `accounts/accounts.txt`
2. Загрузит все изображения из папки `avatars/`
3. Случайным образом назначит аватарки на аккаунты
4. Авторизуется на каждом аккаунте
5. Установит новую аватарку
6. Выйдет из аккаунта
7. Подождет установленную задержку (по умолчанию 60 секунд)

### Аргументы командной строки

Программа поддерживает следующие аргументы:

```bash
# Изменить задержку между аккаунтами (в секундах)
python main.py --delay 120  # или -d 120

# Использовать другой файл конфигурации
python main.py --config custom_config.ini  # или -c custom_config.ini

# Отключить таймер обратного отсчета
python main.py --no-countdown

# Комбинация аргументов
python main.py -d 90 --no-countdown
```

**Примеры:**

```bash
# Быстрый режим (30 секунд между аккаунтами)
python main.py -d 30

# Безопасный режим (2 минуты между аккаунтами)
python main.py -d 120

# Без визуального таймера
python main.py --no-countdown
```

### Пример вывода

```
============================================================
  Steam Avatar Auto-Changer
  Автоматическая смена аватарок в Steam
============================================================

🔄 Загрузка аккаунтов...
✓ Загружено аккаунтов: 35

🔄 Загрузка аватарок...
✓ Найдено аватарок: 40

📊 Статистика:
   Загружено аккаунтов: 35
   Загружено аватарок: 40
   Задержка между аккаунтами: 60 сек (1 мин 0 сек)

🎲 Выбор случайных аватарок для аккаунтов...

⏱️  Примерное время выполнения: 34мин 0сек
🎯 Ожидаемое время завершения: 16:45:30

────────────────────────────────────────────────────────────
[1/35] Обработка аккаунта: mysteamlogin
  🔐 Авторизация...
  ✓ Успешная авторизация: mysteamlogin
  🖼️  Установка аватарки: avatar12.jpg
  ✓ Аватарка успешно изменена!
  ⏳ До следующего аккаунта (2/35): [████████░░░░░░░░░░░░░░░░░░░░] 00:42
  ✓ Ожидание завершено!

────────────────────────────────────────────────────────────
[2/35] Обработка аккаунта: testuser
  🔐 Авторизация...
  ✓ Успешная авторизация: testuser
  🖼️  Установка аватарки: avatar23.png
  ✓ Аватарка успешно изменена!
  ⏳ До следующего аккаунта (3/35): [█████████████████░░░░░░░░░░░░░] 00:25
...

============================================================
📈 Итоговая статистика:
   ✓ Успешно: 33
   ✗ Ошибок: 2
   📊 Процент успеха: 94.3%
============================================================

✓ Программа завершена!
```

## Решение проблем

**Столкнулись с проблемой?** Смотрите подробное руководство: [TROUBLESHOOTING.md](TROUBLESHOOTING.md)

### Ошибка авторизации

**Проблема:** `✗ Неверные учетные данные`

**Решение:**
- Проверьте правильность логина и пароля
- Убедитесь, что формат файла правильный (username:password)
- Если включен Steam Guard, добавьте shared secret

### Не найдены аватарки

**Проблема:** `✗ Не найдено аватарок!`

**Решение:**
- Убедитесь, что изображения находятся в папке `avatars/`
- Проверьте формат файлов (JPG, PNG, GIF, BMP)

### Ошибка смены аватарки

**Проблема:** `✗ Не удалось изменить аватарку`

**Решение:**
- Проверьте размер изображения
- Убедитесь, что файл не поврежден
- Проверьте логи в `logs/app.log` для подробностей

### Steam Guard

Для аккаунтов с включенным Steam Guard Mobile Authenticator необходимо указать shared secret в формате:
```
username:password:shared_secret
```

Чтобы получить shared secret, вы можете использовать инструменты вроде [Steam Desktop Authenticator](https://github.com/Jessecar96/SteamDesktopAuthenticator).

## Ограничения

- Программа работает только с Steam аккаунтами
- Требуется активное подключение к интернету
- Steam может ограничивать частоту смены аватарок
- Рекомендуется задержка между операциями (по умолчанию 60 секунд)
- Не рекомендуется использовать слишком маленькую задержку (меньше 30 секунд), чтобы избежать блокировки Steam

## Техническая информация

### Как работает программа?

Программа использует **Selenium WebDriver** для автоматизации браузера Chrome:

1. **Авторизация**: Открывается страница входа Steam, автоматически вводятся логин и пароль
2. **Steam Guard**: Если требуется код 2FA, программа генерирует его из `shared_secret` используя алгоритм TOTP (Time-based One-Time Password)
3. **Смена аватарки**: После успешного входа браузер переходит на страницу редактирования профиля и загружает новую аватарку
4. **Безопасность**: Все действия выполняются в реальном браузере, как если бы вы делали это вручную

### Headless режим

По умолчанию браузер работает в **headless режиме** (без GUI) - вы не увидите окно браузера. Это:
- Быстрее и потребляет меньше ресурсов
- Позволяет работать на серверах без графического интерфейса
- Не отвлекает визуально

Если хотите видеть процесс в реальном времени для отладки, измените в `src/steam_manager.py`:
```python
steam_manager = SteamManager(headless=False)
```

### Как отключить таймер обратного отсчета?

Есть два способа:
1. В файле `config.ini` установите `show_countdown = false`
2. Используйте аргумент `--no-countdown` при запуске: `python main.py --no-countdown`

### Сколько времени занимает обработка всех аккаунтов?

Программа автоматически рассчитывает и показывает примерное время выполнения при запуске.


## Отказ от ответственности

Эта программа создана исключительно в образовательных целях. Автор не несет ответственности за любое неправомерное использование или нарушение условий использования Steam. Используйте на свой страх и риск.

## Поддержка

Если у вас возникли проблемы или вопросы, создайте issue в репозитории GitHub.

---

